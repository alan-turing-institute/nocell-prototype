language: minimal

dist: xenial

install:
  - sudo add-apt-repository -y ppa:plt/racket
  - sudo apt-get update
  - sudo apt-get install -y racket racket-doc

script:
  - raco pkg install --auto
  - raco test .
