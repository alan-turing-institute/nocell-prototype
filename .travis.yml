language: minimal

dist: xenial
addons:
  apt:
    sources:
      - sourceline: 'ppa:plt/racket'
    packages:
      - racket

script:
  - raco pkg install racket-doc --auto
  - raco pkg install --auto
  - raco test .
