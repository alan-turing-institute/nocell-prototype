language: minimal

dist: xenial

install:
  - sudo add-apt-repository -y ppa:plt/racket
  - sudo apt-get update
  - sudo apt-get install -y racket=7.1 racket-doc

script:
  - raco pkg install --auto
  - raco test .
